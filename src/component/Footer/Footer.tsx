import React from "react";
import labels from "../../labels";
import "./Footer.scss";

const Footer = () => {
  return (
    <footer>
      <h3>{labels.footer.text}</h3>
    </footer>
  );
};

export default Footer;
